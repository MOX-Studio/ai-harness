# Плагины

Список пока пуст. Добавляйте сюда пакеты или инструкции подключения, а в `catalog.json → plugins` — записи с `id`, `instructions`, `clients` и `version`. Укажите источник, установку, обновление и проверку доступности в поддерживаемых клиентах.
